import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The lander is a single fixed viewport — the dev badge sits on top of the footer.
  devIndicators: false,
};

export default nextConfig;
